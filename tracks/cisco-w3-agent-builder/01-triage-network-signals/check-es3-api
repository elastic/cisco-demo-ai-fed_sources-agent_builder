#!/bin/bash
# Always-pass Instruqt check/solve for hostname es3-api.
# Keep this minimal: no set -e/-u, ASCII-only. Infrastructure errors
# ("Something went wrong while checking") usually mean the VM was not ready
# or the script was not executable, not a failed assertion.
echo "OK - continue when you have completed the tasks in Instructions."
exit 0
